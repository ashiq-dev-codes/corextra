# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/
build/

# Generated by `flutter pub get` now that this package registers its own
# Android plugin code (see android/) and so resolves a plugin list itself.
.flutter-plugins
.flutter-plugins-dependencies

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock

# ...but the example is a runnable app, not a library — commit its lock
# file for reproducible builds.
!/example/pubspec.lock

# Claude Code local settings
.claude/hooks
.claude/settings.json
.claude/settings.local.json